[package]
name = "pith-virtual-core"
description = "Framework-agnostic virtual scrolling core (Rust port of @tanstack/virtual-core)."

authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
version.workspace = true

[dependencies]
# None — pure Rust, no web-sys or framework dependencies.

[dev-dependencies]
